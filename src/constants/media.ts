export const MAX_FILE_SIZE = 500 * 1024 * 1024; // 500MB
export const MAX_FILES = 99;

export const SUPPORTED_FORMATS = {
    image: 'APNG, WebP, PNG, JPEG, GIF, JPG, BMP, SVG, TIFF, ICO',
    video: 'MP4, WebM, OGG, MPEG, QuickTime, FLV, AVI'
};